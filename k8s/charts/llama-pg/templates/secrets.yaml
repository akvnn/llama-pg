apiVersion: v1
kind: Secret
metadata:
  name: {{ include "llama-pg.fullname" . }}-secret
  labels:
    app.kubernetes.io/name: {{ include "llama-pg.name" . }}
    helm.sh/chart: {{ include "llama-pg.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
type: Opaque
data:
{{- if .Values.secretEnv }}
{{- range $key, $val := .Values.secretEnv }}
  {{ $key }}: {{ $val | quote }}
{{- end }}
{{- end }}
