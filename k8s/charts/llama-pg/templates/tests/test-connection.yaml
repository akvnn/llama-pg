apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "llama-pg.fullname" . }}-test-connection"
  labels:
    {{- include "llama-pg.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "llama-pg.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
